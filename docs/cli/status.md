# `claw status`

Show repository and working-tree status.

```bash
claw status
claw status --json
```

Status reports the current branch, pending worktree changes, and repository state. Use JSON output for scripts.
