# `claw resolve`

Manage merge conflicts created by integration workflows.

```bash
claw resolve list
claw resolve show <conflict-id>
claw resolve mark <conflict-id> --status resolved
```

Conflict commands are intended for review tools and agents that need explicit conflict state rather than parsing merge text.
